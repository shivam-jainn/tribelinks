{
  "name": "tracker-server",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "nodemon --watch src -e ts --exec ts-node src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/static": "^7.0.0",
    "@tracker/core": "file:../../packages/core",
    "@tracker/queue": "file:../../packages/queue",
    "dotenv": "^17.4.2",
    "fastify": "^4.25.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "@types/pg": "^8.11.6"
  }
}
